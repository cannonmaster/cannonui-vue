module.exports = function (wallaby) {
  return {
    files: [
      //   'src/**/*.js'
    ],

    tests: [
      // 'src/cmpStore/**/*.spec.{ts,tsx,js,jsx}'
    ],
    // for node.js tests you need to set env property as well
    // https://wallabyjs.com/docs/integration/node.html
  };
};
