---
title: 'Drag Component'
description: 'drag component Docs intro'
layout: '@/layouts/MainLayout.astro'
cname: 'drag'
---

import DragVue from '@/cmpStore/Drag/vue/demo.vue';

This component allows you to drag an element in a specified direction and boundary.

### Example Usage

<DragVue client:visible />

Here’s an example of how to use the Drag component:

```vue
<script lang="ts">
import { onMounted, reactive, ref } from 'vue';
export default {
  props: {
    lang: {
      type: String,
      default: 'en'
    }
  },
  emits: [],
  components: {},
  setup(props) {}
};
</script>

<template>
  <div>
    <cannonui-drag direction="all">
      <span style="padding: 10px; background: cyan">drag</span>
    </cannonui-drag>
  </div>
  <div>
    <cannonui-drag direction="x">
      <span style="padding: 10px 20px; background: pink">x</span>
    </cannonui-drag>
  </div>
  <div>
    <cannonui-drag direction="y">
      <span style="padding: 10px 20px; background: orange">y</span>
    </cannonui-drag>
  </div>
  <div>
    <cannonui-drag
      attract
      direction="all"
    >
      <span style="padding: 10px 20px; background: cyan">abc123</span>
    </cannonui-drag>
  </div>
</template>

<style scoped lang="scss"></style>
```

### Props

| Prop      | Type   | Description                                                                          |
| --------- | ------ | ------------------------------------------------------------------------------------ |
| attract   | bool   | Specifies whether to attract the dragged element to the boundary or not.             |
| direction | string | Specifies the direction in which the element can be dragged. Default value is 'all'. |
| style     | string | Specifies the style of the element.                                                  |
| boundary  | object | An object that specifies the boundary of the dragged element.                        |
